============================================================

💡 [최종 스캐너 적용 수식 (Python)]
1. z = -7.8119 + (0.1985 * base_cvss) + (5.7956 * base_epss) + (1.0100 * flag_poc) + (0.0649 * flag_cwe) 
2. final_prob = 1 / (1 + np.exp(-z))  # Sigmoid 적용
============================================================

cwe 포함해서 학습시 flag_cwe의 계수가 미미함(전체 예측 중 차지하는 비율 0.9%)을 알 수 있습니다.
현재 수식에서 지배적인 값들은 epss와 poc 값으로, 저 두 값만 가지고도 대부분의 예측을 할 수 있는 수준입니다.
따라서 cwe를 학습에 포함시키는 경우, '은행권에서 사용을 상정하고 학습했다' 정도의 의의만을 가집니다.
